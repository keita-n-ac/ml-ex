# 2025年度 レポート3（締め切り：2026/1/29）

- 以下の問題に示すプログラムを作成しなさい（合計4問）
- 各問を満たすプログラム・散布図をwordに貼り付けて，提出すること

### 問題1
- 以下の題意を満たすプログラムを作成しなさい

1: パラメータを以下のようにした塊分布の自作データを作成する
  - シードを20，特徴量を2次元，目標を2つ（塊を2つ），データの標準偏差を1.0，データ数を600で作成する

2: 1で作成したテストデータを10%になるように分割する（シードを5，学習データは90%とする）

3: サポートベクターマシンモデルを作成し， 訓練データを使用して，サポートベクターマシンモデルの学習を行い，学習後のモデルに対して，特徴量テストデータを適用し，正答率を出力せよ．

### 問題2
- 以下の題意を満たす散布図を作成しなさい
- 問題1のプログラムを利用して，塊分布の自作データの標準偏差を1.0，2.0，3.0，･･･，20.0とした場合におけるそれぞれの正答率を求め，横軸を標準偏差，縦軸を正答率とした散布図を作成せよ
- 問題1のプログラムを変更する箇所は塊分布の自作データの標準偏差だけで問題ない
- 正答率をプログラムで求めて，紙にメモして，matplotlibで作成しても問題ない，または紙にメモした値を使用して，excelで作成しても問題ない．

### 問題3
- 以下の題意を満たすプログラムを作成しなさい

1: パラメータを以下のようにした線形回帰の自作データを作成する
  - シードを3，特徴量1つ，ノイズの度合いを5，データ数を400で作成する

2: 1で作成したテストデータを30%になるように分割する（シードを15，学習データは70%とする）

3: 線形回帰モデルを作成し，訓練データを使用して，線形回帰モデルの学習を行う

4: 学習後のモデルに対して，特徴量テストデータを適用し，平均二乗誤差の値を出力せよ

### 問題4
- 以下の題意を満たす散布図を作成しなさい
- 問題3のプログラムを利用して，線形回帰の自作データのノイズの度合いを0.0，5.0，10.0，15.0，･･･，100.0とした場合におけるそれぞれの平均二乗誤差の値を求め，横軸をノイズの度合い，縦軸を平均二乗誤差とした散布図を作成せよ
- 問題3のプログラムを変更する箇所は線形回帰の自作データのノイズの度合いだけで問題ない
- 平均二乗誤差をプログラムで求めて，紙にメモして，matplotlibで作成しても問題ない，または，紙にメモした値を使用して，excelで作成しても問題ない

